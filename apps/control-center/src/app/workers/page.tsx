'use client'
import {useEffect,useState} from 'react'
type Worker={name:string;status:'online'|'degraded'|'offline';requests:string;latency:string;errors:string;updated:string}
const initial:Worker[]=[{name:'marketing',status:'online',requests:'—',latency:'—',errors:'—',updated:'Waiting for Cloudflare API'},{name:'control-center',status:'online',requests:'—',latency:'—',errors:'—',updated:'Waiting for Cloudflare API'},{name:'workspace',status:'online',requests:'—',latency:'—',errors:'—',updated:'Waiting for Cloudflare API'}]
export default function Workers(){const[data,setData]=useState(initial);const[live,setLive]=useState(false);useEffect(()=>{const t=setInterval(()=>setData(x=>x.map(w=>({...w,updated:'Just now'}))),10000);return()=>clearInterval(t)},[]);return <main className="min-h-screen bg-slate-950 p-6 text-white md:p-10"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm text-slate-500">Briqona Control Center</p><h1 className="mt-1 text-3xl font-semibold">Workers</h1><p className="mt-2 text-slate-400">Live runtime view for your Cloudflare Workers.</p></div><div className="flex items-center gap-3"><span className={`flex items-center gap-2 text-sm ${live?'text-emerald-400':'text-slate-500'}`}><i className="h-2 w-2 rounded-full bg-current"/>{live?'Live':'Preview'}</span><button onClick={()=>setLive(v=>!v)} className="rounded-lg border border-slate-700 px-4 py-2 text-sm">{live?'Stop live':'Enable live view'}</button></div></div><div className="mt-8 grid gap-4 md:grid-cols-4">{[['Workers','3'],['Requests','—'],['Errors','—'],['Avg latency','—']].map(x=><div key={x[0]} className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><p className="text-sm text-slate-500">{x[0]}</p><p className="mt-2 text-2xl font-semibold">{x[1]}</p></div>)}</div><section className="mt-6 overflow-hidden rounded-2xl border border-slate-800 bg-slate-900"><div className="border-b border-slate-800 p-5"><h2 className="font-semibold">Worker health</h2></div><div className="divide-y divide-slate-800">{data.map(w=><div key={w.name} className="grid gap-4 p-5 md:grid-cols-6 md:items-center"><div><p className="font-medium">{w.name}</p><p className="text-xs text-slate-500">Cloudflare Worker</p></div><span className="text-sm text-emerald-400">● {w.status}</span><div><p className="text-xs text-slate-500">Requests</p><p>{w.requests}</p></div><div><p className="text-xs text-slate-500">Latency</p><p>{w.latency}</p></div><div><p className="text-xs text-slate-500">Errors</p><p>{w.errors}</p></div><p className="text-xs text-slate-500">{w.updated}</p></div>)}</div></section><div className="mt-5 rounded-xl border border-amber-900/40 bg-amber-950/20 p-4 text-sm text-amber-300">Live request, latency and error metrics require Cloudflare API credentials and Worker analytics bindings. The UI is ready; payment APIs remain untouched.</div></div></main>}