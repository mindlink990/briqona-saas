'use client'
import {useEffect,useState} from 'react'
type Finance={yesterday:{revenue:number;expense:number;refund:number;profit:number};today:{revenue:number;expense:number;refund:number;profit:number};change:{revenue:number;expense:number;refund:number;profit:number}}
export default function FinanceCard(){const[d,setD]=useState<Finance|null>(null);useEffect(()=>{fetch('/api/admin/ai/finance').then(r=>r.json()).then(setD)},[]);if(!d)return <div className="rounded-2xl border border-slate-800 bg-slate-900 p-6 text-slate-500">Loading financial insight…</div>;const money=(n:number)=>`$${n.toFixed(2)}`;return <section className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><div className="flex items-center justify-between"><div><h2 className="font-semibold">📊 Yesterday → Today</h2><p className="mt-1 text-sm text-slate-500">Actual transaction ledger</p></div><span className={d.change.profit>=0?'text-emerald-400':'text-red-400'}>{d.change.profit>=0?'+':''}{money(d.change.profit)} profit</span></div><div className="mt-5 grid gap-3 sm:grid-cols-4">{[['Revenue',d.today.revenue,d.change.revenue],['Expenses',d.today.expense,d.change.expense],['Refunds',d.today.refund,d.change.refund],['Net Profit',d.today.profit,d.change.profit]].map(([label,value,change])=><div key={String(label)} className="rounded-xl border border-slate-800 p-4"><p className="text-xs text-slate-500">{label}</p><p className="mt-2 text-xl font-semibold">{money(Number(value))}</p><p className={`mt-1 text-xs ${Number(change)>=0?'text-emerald-400':'text-red-400'}`}>{Number(change)>=0?'+':''}{money(Number(change))} vs yesterday</p></div>)}</div></section>}
